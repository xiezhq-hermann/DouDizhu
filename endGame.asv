function endGame(players)

for player = players
    if (player.board.takeDizhu == 1)
        players = player.board.players;
        break;
    end
end
    
    pause(10)

    players(1).delete
    players(2).delete
    players(3).delete

end